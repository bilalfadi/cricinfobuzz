NEXT_PUBLIC_SITE_URL=https://cricinfobuzz.n123movie.xyz
# Leave empty to use the built-in Next.js API routes (same origin).
# Set to http://localhost:3002 if you run the standalone Express API server.
NEXT_PUBLIC_API_URL=
